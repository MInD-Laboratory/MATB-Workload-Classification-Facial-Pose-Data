# Example Environment Configuration
# Copy this file to .env and update paths for your local setup
#
# For local development with data in a different location:
#   cp .env.example .env
#   # Edit .env with your actual paths
#
# For published/shared usage (data in standard location):
#   Don't create a .env file - defaults will be used
#   Data should be in: [root]/data/pose_data/, [root]/data/gsr_data/, etc.

# Path to pose data CSV files
# Default (if not set): [root]/Pose/data/pose_data
# POSE_RAW_DIR=/path/to/your/data/pose_data

# Path for processed output files
# Default (if not set): [root]/Pose/data/processed
# POSE_OUT_BASE=/path/to/your/output/processed

# Participant info CSV filename
# Default (if not set): participant_info.csv
# (Located in parent directory of POSE_RAW_DIR or in data/)
# PARTICIPANT_INFO_FILE=participant_info.csv

# Example for macOS with OneDrive:
# POSE_RAW_DIR=/Users/username/Library/CloudStorage/OneDrive-SharedLibraries-Organization/Project/data/pose_data
# POSE_OUT_BASE=/Users/username/Projects/MATB/Pose/data/processed

# Example for Windows with OneDrive:
# POSE_RAW_DIR=C:/Users/username/OneDrive - Organization/Project/data/pose_data
# POSE_OUT_BASE=C:/Users/username/Projects/MATB/Pose/data/processed
